
<div class="item">
    <div>Nome</div>
    <div>
        <input 
            type="text" 
            name="name" 
            value="{{chef.name}}"
            placeholder="Nome do chef"
        >
    </div>
</div>
<div class="item">
    <div>Avatar</div>
    <div>
        <input 
            type="URL" 
            name="avatar_url" 
            value="{{chef.avatar_url}}"
            placeholder="htpp://"
        >
    </div>
</div>
<input type="hidden" name="id" value="{{chef.id}}">